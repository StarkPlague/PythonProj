# membuat att class
class NamaKelas:
    # att kelas (jika kita merubah isian dari att kelas, semua akan berubah)
    namaAtributkelas = "dataAtributKelas"
    # magic func kontruktor
    # type casting = merubah tipe data

    def __init__(self, atObj):
        self.atributObjek = atObj
